<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.agripath.mapper.MapImageMapper">

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        insert into map_image (uuid_name, origin_name, width, height, file_size, thumb_size, created_at)
        values (#{uuidName}, #{originName}, #{width}, #{height}, #{fileSize}, #{thumbSize}, #{createdAt})
    </insert>

    <select id="getById" resultType="com.agripath.entity.MapImage">
        select * from map_image where id = #{id}
    </select>

    <select id="listAll" resultType="com.agripath.entity.MapImage">
        select * from map_image order by created_at desc
    </select>
</mapper>
